%head
  -# %title= "Journal#{' - ' + current_article.title unless current_article.nil?}"

  = current_page.data.title
  %meta{ http_equiv: "X-UA-Compatible", content: "IE=edge,chrome=1" }
  %meta{ charset: "utf-8" }
  %meta{ content: "width=device-width, initial-scale=1", name: "viewport" }

  = stylesheet_link_tag 'application'
  = stylesheet_link_tag "//fonts.googleapis.com/css?family=Open+Sans:400,600,700' rel='stylesheet' type='text/css'"